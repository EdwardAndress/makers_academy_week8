<%= form_for :restaurant, url: restaurants_path do |f| %>
	<p>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :cuisine %><br>
		<%= f.text_field :cuisine %>
	</p>
	<p>
		<%= f.submit %>
	</p>

<% end %>