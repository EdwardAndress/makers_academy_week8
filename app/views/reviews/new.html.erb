<%= form_for [@restaurant, @review] do |f| %>
<p>
	<%= f.label :comments %><br>
	<%= f.text_field :comments %>
	
	<%= f.label :rating %>
	<%= f.select :rating, (1..5) %>
	<%= f.submit 'Submit review'%>
</p>
<% end %>